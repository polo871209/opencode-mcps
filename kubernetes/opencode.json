{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "kubernetes-mcp-server": {
      "enabled": true,
      "type": "local",
      "command": [
        "npx",
        "-y",
        "kubernetes-mcp-server@latest",
        "--read-only"
      ]
    }
  }
}
