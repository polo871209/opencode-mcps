# opencode-mcp

Personal workspace for MCP server configurations and agent setups to streamline daily tasks.

## Structure

- **kubernetes/** - K8s troubleshooting workspace with read-only cluster analysis and remediation suggestions
- **promethues/** - Prometheus metrics querying and monitoring setup

Each directory contains its own `opencode.json` configuration for MCP servers and agent guidelines for automated workflows.
